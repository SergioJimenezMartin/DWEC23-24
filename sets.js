let conjunto = new Set ("Pamplina");
//se recorre con u8n for of

let conjuntos = new Set(([1, 2, 3, 5, 6, 3, 2, 7]))
for (let elem of conjuntos){
    console.log(elem)
}


const miarray = miarray[1, 2, 3, 4, 3, 2, 1]
let miset = new set [miarray];
let miarrayfinal = []
